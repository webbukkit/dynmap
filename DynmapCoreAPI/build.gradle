description = "DynmapCoreAPI"

jar {
    destinationDir = file '../target'
}

artifacts {
    archives jar
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}
